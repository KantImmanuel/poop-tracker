{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd server && npm install",
    "watchPatterns": ["server/**", "railway.json"]
  },
  "deploy": {
    "startCommand": "cd server && npx prisma generate && npx prisma db push && node index.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
